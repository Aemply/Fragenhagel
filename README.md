# Fragenhagel v88
Basis: v87. Geo-Bild-Upload verwendet dieselbe Editor-/Host-Autorisierung wie das normale Editor-Speichern und Face-Morph-Bild-Upload. Dadurch funktionieren Geo-Bild-Uploads auch nach einer neuen Lobby zuverlässig.


## v99 – Manuelle Punkte
+100 und -100 laufen über einen geschützten HTTP-Host-Endpunkt statt ausschließlich über den Socket. Die neue Punktzahl wird nach erfolgreicher Antwort sofort lokal angezeigt und zusätzlich per Game-State synchronisiert.
